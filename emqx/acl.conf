%% Development ACL — allow all publish/subscribe
%% EMQX evaluates rules top-to-bottom; first match wins.
%% This dev config allows everything so external clients (MQTT Explorer,
%% Node-RED, etc.) can subscribe with wildcards (#, +) without restriction.

{allow, all}.
